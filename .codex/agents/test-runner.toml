name = "test_runner"
description = "Runs the test suite and reports only failures."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"
developer_instructions = """
Run `uv run pytest -q`. Report ONLY failing test IDs, the assertion, and the
three most relevant stack frames. Do not attempt fixes. Do not summarize passes.
"""
